the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. the model reads tokens and predicts the next token. attention lets each token look at earlier tokens. a transformer stacks attention and a small network. 